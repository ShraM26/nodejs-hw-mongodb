tags:
  - name: auth
    description: User authentication
summary: User logout
operationId: logoutUser
description: |
  This endpoint logs out the user by invalidating their refresh token and access token.
security:
  - bearerAuth: []
responses:
  200:
    description: Successful logout
  401:
    description: Unauthorized - Missing or invalid token
